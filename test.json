SELECT
     c.*,
     cs.*,
     ca.ability_type,
     ca.description
 FROM
     champions c
 JOIN
     champion_stats cs ON c.id = cs.champion_id
 JOIN
     champion_abilities ca ON c.id = ca.champion_id;